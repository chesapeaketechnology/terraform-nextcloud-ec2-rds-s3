# The name of the S3 bucket where the state will be stored
bucket = "nextcloud-datastore"

# The region of the S3 bucket
region = "us-gov-west-1"

# Key to the state file inside the bucket
key = "network/terraform.tfstate"
